const {modules,cases,flashcards}=window.IFSI_DATA;
const STORAGE='ifsi-coach-ue25-v1',base={moduleScores:{},cardsSeen:[],casesDone:[],lastDiagnosticTopic:null,lastResult:null,streak:1};
let state=load(),participant=null,topic='',questions=[],qi=0,choice=null,validated=false,answers=[],cards=[],ci=0,revealed=false,caseIndex=0,caseChoice=null,caseValidated=false,cat='Tous',dcat='Tous',ccat='Tous',syncTimer=null,pendingTopic=null;
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
function load(){try{return{...base,...JSON.parse(localStorage.getItem(STORAGE)||'{}')}}catch{return{...base}}}
function save(attempt=null){localStorage.setItem(STORAGE,JSON.stringify(state));refresh();if(attempt)pendingTopic=attempt;clearTimeout(syncTimer);syncTimer=setTimeout(sync,250)}
async function sync(){if(!participant)return;try{await fetch('/api/progress',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({moduleScores:state.moduleScores||{},cardsSeen:state.cardsSeen||[],casesDone:state.casesDone||[],lastDiagnosticTopic:state.lastDiagnosticTopic||null,attemptedTopic:pendingTopic})});pendingTopic=null;$('#sidebar-status').textContent='Progression enregistrée pour la promo'}catch{$('#sidebar-status').textContent='Sauvegarde locale · synchronisation en attente'}}
function apply(p){participant=p;const n=p.firstName||'Étudiant';$('#profile-name').textContent=n;$('#profile-cohort').textContent=p.cohortLabel||'Saintes 25–28';$('#profile-avatar').textContent=n.slice(0,2).toUpperCase();$('#welcome-label').textContent=`BONJOUR ${n.toUpperCase()} · SESSION DU JOUR`;$('#sidebar-status').textContent='Progression enregistrée pour la promo'}
async function initSession(){try{const r=await fetch('/api/session');if(!r.ok){$('#join-gate').hidden=false;return}const d=await r.json();apply(d.participant);if(d.state){state={...state,...d.state,moduleScores:d.state.moduleScores||{},cardsSeen:d.state.cardsSeen||[],casesDone:d.state.casesDone||[]};localStorage.setItem(STORAGE,JSON.stringify(state))}refresh()}catch{$('#join-gate').hidden=false}}
async function join(e){e.preventDefault();const b=e.currentTarget.querySelector('button[type=submit]'),err=$('#join-error');err.textContent='';b.disabled=true;try{const r=await fetch('/api/join',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({firstName:$('#join-name').value.trim(),password:$('#join-cohort').value})});const d=await r.json();if(!r.ok)throw Error(d.error||'Connexion impossible');apply(d.participant);$('#join-gate').hidden=true;if(d.state)state={...state,...d.state};save()}catch(x){err.textContent=x.message}finally{b.disabled=false}}
function show(id){$$('.view').forEach(v=>v.classList.toggle('active',v.id===id));$$('.nav-item').forEach(n=>n.classList.toggle('active',n.dataset.view===id));$$('.mobile-nav-item').forEach(n=>n.classList.toggle('active',n.dataset.mobileView===id));const l={home:"Aujourd'hui",diagnostic:'Diagnostic',quiz:'Diagnostic',learn:'Réviser',case:'Cas clinique',results:'Ma progression'};$('#crumb').innerHTML=`S3 <span>›</span> UE 2.5 <span>›</span> ${l[id]||'Processus infectieux'}`;if(id==='diagnostic')renderDiagnostics();if(id==='learn')renderModules();if(id==='case')renderCases();if(id==='results')renderResults();if(id==='home')refresh();scrollTo({top:0,behavior:'smooth'})}
function refresh(){const scores=Object.values(state.moduleScores||{});$('#home-score').textContent=scores.length?Math.round(scores.reduce((a,b)=>a+b,0)/scores.length)+'%':'À évaluer';$('#home-questions').textContent=String(scores.length*10);$('#home-cards').textContent=`${state.cardsSeen.length} / ${flashcards.length}`;$('#learn-total-value').textContent=Math.round(state.cardsSeen.length/flashcards.length*100)+'%';$('#case-complete-count').textContent=state.casesDone.length;$('#case-total-count').textContent=cases.length;$('#diag-complete-value').textContent=`${scores.length}/${Object.keys(modules).length}`;$('#streak-value').textContent=`${state.streak||1} jour${(state.streak||1)>1?'s':''}`}
function filters(el,active,setter,kind){const vals=['Tous',...new Set(Object.values(modules).map(m=>m.category))];el.innerHTML=vals.map(v=>`<button class="filter-chip ${v===active?'active':''}" data-${kind}="${v}">${v}</button>`).join('');$$(`[data-${kind}]`).forEach(b=>b.onclick=()=>setter(b.dataset[kind]))}
function renderModules(){filters($('#module-filters'),cat,v=>{cat=v;renderModules()},'mf');const entries=Object.entries(modules).filter(([,m])=>cat==='Tous'||m.category===cat);$('#module-grid').innerHTML=entries.map(([t,m],i)=>`<button class="module-card" data-topic="${t}" style="--topic:${m.color}"><span class="module-category">${m.category}</span><span class="module-num">${String(i+1).padStart(2,'0')}</span><h3>${t}</h3><p>${m.short}</p><div class="module-foot"><span>10 cartes · ouvrir →</span><span>${state.moduleScores?.[t]??'—'}${state.moduleScores?.[t]!==undefined?'%':''}</span></div></button>`).join('');$$('.module-card').