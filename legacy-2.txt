forEach(b=>b.onclick=()=>openModule(b.dataset.topic))}
function openModule(t){const m=modules[t];cards=flashcards.filter(c=>c.topic===t);ci=0;revealed=false;show('learn');$('#study-stage').hidden=false;$('#study-tag').textContent=`${m.category.toUpperCase()} · 10 CARTES`;$('#study-title').textContent=t;$('#lesson-panel').innerHTML=Object.entries(m.sections).map(([h,it],i)=>`<details class="lesson-section" ${i?'':'open'}><summary>${h}</summary><ul>${it.map(x=>`<li>${x}</li>`).join('')}</ul></details>`).join('')+`<div class="alert-box"><b>Signal d’alerte :</b> ${m.alert}</div>`;renderFlash();$('#study-stage').scrollIntoView({behavior:'smooth'})}
function renderFlash(){const c=cards[ci];revealed=false;$('#flashcard').classList.remove('revealed');$('#flash-progress').textContent=`Carte ${ci+1} / ${cards.length}`;$('#flash-front').textContent=c.front;$('#flash-front').hidden=false;$('#flash-back').textContent=c.back;$('#flash-back').hidden=true;$('#flashcard .eyebrow').textContent='QUESTION'}
function flip(){revealed=!revealed;$('#flash-front').hidden=revealed;$('#flash-back').hidden=!revealed;$('#flashcard').classList.toggle('revealed',revealed);$('#flashcard .eyebrow').textContent=revealed?'RÉPONSE':'QUESTION'}
function nextCard(known){const c=cards[ci];if(known&&!state.cardsSeen.includes(c.id)){state.cardsSeen.push(c.id);save()}ci=(ci+1)%cards.length;renderFlash()}
function build(t){const arr=flashcards.filter(c=>c.topic===t),topics=Object.keys(modules);return arr.map((c,i)=>{const wrong=[];for(let o=1;o<topics.length&&wrong.length<3;o++){const x=flashcards.filter(z=>z.topic===topics[(topics.indexOf(t)+i+o)%topics.length])[i]?.back;if(x&&x!==c.back&&!wrong.includes(x))wrong.push(x)}const ok=i%4,a=[...wrong];a.splice(ok,0,c.back);return{q:c.front,a,ok,why:c.back}})}
function renderDiagnostics(){filters($('#diagnostic-filters'),dcat,v=>{dcat=v;renderDiagnostics()},'df');const e=Object.entries(modules).filter(([,m])=>dcat==='Tous'||m.category===dcat);$('#diagnostic-grid').innerHTML=e.map(([t,m])=>`<article class="diag-card" style="--topic:${m.color}"><span class="module-category">${m.category}</span><h3>${t}</h3><p>${m.short}</p><strong>${state.moduleScores?.[t]!==undefined?state.moduleScores[t]+'%':'Non évalué'}</strong><button class="primary-btn" data-diag="${t}">Commencer · 10 questions <span>→</span></button></article>`).join('');$$('[data-diag]').forEach(b=>b.onclick=()=>start(b.dataset.diag))}
function start(t){topic=t;questions=build(t);qi=0;answers=[];state.lastDiagnosticTopic=t;show('quiz');renderQ()}
function renderQ(){const x=questions[qi];choice=null;validated=false;$('#question-kicker').textContent=`QUESTION ${qi+1}`;$('#question-text').textContent=x.q;$('#topic-chip').textContent=topic;$('#progress-label').textContent=`Question ${qi+1} sur 10`;$('#progress-bar').style.width=((qi+1)*10)+'%';$('#feedback').className='feedback';$('#feedback').textContent='';$('#next-btn').disabled=true;$('#next-btn').textContent='Valider la réponse';$('#answers').innerHTML=x.a.map((v,i)=>`<button class="answer" data-a="${i}"><span>${'ABCD'[i]}</span>${v}</button>`).join('');$$('[data-a]').forEach(b=>b.onclick=()=>{if(validated)return;choice=+b.dataset.a;$$('.answer').forEach((z,i)=>z.classList.toggle('selected',i===choice));$('#next-btn').disabled=false})}
function validateQ(){if(choice===null)return;const x=questions[qi];if(!validated){validated=true;answers.push(choice===x.ok);$$('.answer').forEach((b,i)=>{if(i===x.ok)b.classList.add('correct');if(i===choice&&i!==x.ok)b.classList.add('wrong')});$('#feedback').className='feedback show '+(choice===x.ok?'good':'bad');$('#feedback').innerHTML=`<b>${choice===x.ok?'Bonne réponse.':'À revoir.'}</b> ${x.why}`;$('#next-btn').textContent=qi===9?'Voir mon bilan':'Question suivante';return}if(qi<9){qi++;renderQ()}else finishQ()}
function finishQ(){const score=Math.round(answers.filter(Boolean).length*10);state.moduleScores={...(state.moduleScores||{}),[topic]:score};state.lastDiagnosticTopic=topic;state.lastResult={topic,total:score};save(topic);show('results')}
function renderCases(){filters($('#case-filters'),ccat,v=>{ccat=v;renderCases()},'cf');const list=cases.map((c,i)=>({...c,_i:i})).filter(c=>ccat==='Tous'||c.category===ccat);$('#case-picker').innerHTML=list.map(c=>`<button class="case-pick ${c._i===caseIndex?'active':''}" data-case="${c._i}"><span>${c.category}</span><small>${c.topic}</small></button>`).join('');$$('[data-case]').forEach(b=>b.onclick=()=>{caseIndex=+b.dataset.case;renderCases()});const c=cases[caseIndex];$('#case-patient').textContent=c.patient;$('#case-place').textContent=c.place;$('#case-initials').textContent=c.initials;$('#case-context').textContent=c.context;$('#case-category').textContent=c.category.toUpperCase();$('#case-question').textContent=c.question;$('#case-vitals').innerHTML=c.vitals.map(v=>`<div class="${v[2]?'alert':''}"><span>${v[0]}</span><b>${v[1]}</b></div>`).join('');$('#case-signs').innerHTML=c.signs.map(s=>`<span>${s}</span>`).joi