n('');$('#case-answers').innerHTML=c.options.map((o,i)=>`<button class="answer" data-ca="${i}"><span>${'ABCD'[i]}</span>${o}</button>`).join('');caseChoice=null;caseValidated=false;$('#case-next').disabled=true;$('#case-next').textContent='Valider mon choix';$('#case-feedback').className='feedback';$('#case-debrief').hidden=true;$$('[data-ca]').forEach(b=>b.onclick=()=>{if(caseValidated)return;caseChoice=+b.dataset.ca;$$('[data-ca]').forEach((z,i)=>z.classList.toggle('selected',i===caseChoice));$('#case-next').disabled=false})}
function validateCase(){const c=cases[caseIndex];if(caseChoice===null)return;if(!caseValidated){caseValidated=true;$$('[data-ca]').forEach((b,i)=>{if(i===c.ok)b.classList.add('correct');if(i===caseChoice&&i!==c.ok)b.classList.add('wrong')});$('#case-feedback').className='feedback show '+(caseChoice===c.ok?'good':'bad');$('#case-feedback').innerHTML=`<b>${caseChoice===c.ok?'Bonne priorité.':'Priorité à revoir.'}</b> ${c.why}`;$('#case-next').textContent='Voir le débrief';if(!state.casesDone.includes(c.id)){state.casesDone.push(c.id);save()}return}$('#debrief-title').textContent=c.title;$('#debrief-why').textContent=c.debrief[0];$('#debrief-action').textContent=c.debrief[1];$('#debrief-watch').textContent=c.debrief[2];$('#case-debrief').hidden=false}
function renderResults(){const t=state.lastDiagnosticTopic,s=t?state.moduleScores?.[t]:null;$('#score-value').textContent=s!==null&&s!==undefined?s+'%':'—';$('#score-ring').style.setProperty('--score',s||0);$('#result-copy').textContent=t?`${t} : ${s>=75?'bases solides.':s>=50?'acquis à consolider.':'module à reprendre.'}`:'Commence un diagnostic.';$('#mastery-list').innerHTML=Object.keys(modules).map(n=>`<div class="mastery-row"><span>${n}</span><div class="bar"><i style="width:${state.moduleScores?.[n]||0}%;background:${modules[n].color}"></i></div><strong>${state.moduleScores?.[n]!==undefined?state.moduleScores[n]+'%':'—'}</strong></div>`).join('');$('#priority-title').textContent=t?`À consolider : ${t}`:'Diagnostic requis';$('#priority-copy').textContent=t?'Relis la fiche, refais les 10 cartes puis entraîne-toi avec le cas clinique.':'Choisis un module.';$('#plan-items').innerHTML=t?'<div class="plan-item"><i>1</i><span>Fiche de révision</span></div><div class="plan-item"><i>2</i><span>10 cartes</span></div><div class="plan-item"><i>3</i><span>Cas clinique</span></div>':'';$('#review-btn').dataset.topic=t||''}
async function logout(){await fetch('/api/logout',{method:'POST'}).catch(()=>{});localStorage.removeItem(STORAGE);location.reload()}
$('#join-form').onsubmit=join;$('#logout-btn').onclick=logout;$('#start-btn').onclick=()=>show('diagnostic');$('#next-btn').onclick=validateQ;$('#skip-btn').onclick=()=>{choice=-1;validateQ()};$('#quit-btn').onclick=()=>show('diagnostic');$('#flashcard').onclick=flip;$('#again-btn').onclick=()=>nextCard(false);$('#know-btn').onclick=()=>nextCard(true);$('#shuffle-btn').onclick=()=>{cards.sort(()=>Math.random()-.5);ci=0;renderFlash()};$('#close-study').onclick=()=>$('#study-stage').hidden=true;$('#case-next').onclick=validateCase;$('#restart-btn').onclick=()=>state.lastDiagnosticTopic&&start(state.lastDiagnosticTopic);$('#review-btn').onclick=()=>$('#review-btn').dataset.topic?openModule($('#review-btn').dataset.topic):show('diagnostic');$$('.task-button').forEach(b=>b.onclick=()=>b.dataset.action==='flashcards'?openModule(Object.keys(modules)[0]):show(b.dataset.action==='case'?'case':'diagnostic'));$$('.nav-item').forEach(b=>b.onclick=()=>show(b.dataset.view));$$('.mobile-nav-item').forEach(b=>b.onclick=()=>show(b.dataset.mobileView));renderModules();refresh();show('home');initSession();setInterval(()=>participant&&fetch('/api/heartbeat',{method:'POST'}).catch(()=>{}),30000);
